<div class='container'>
  <table class='table table-hover'>
    <thead>
      <tr>
        <th>Top Referrals</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @url.top_three_referrers.each do |referrer, count| %>
      <tr>
        <td><%= referrer %></td>
        <td class="text-right"><%= count %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
