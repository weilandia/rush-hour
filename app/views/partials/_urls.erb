<table class='table table-hover'>
  <thead>
    <tr>
      <th>URL Stats</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @client.urls.ordered_by_requested.each do |k, v| %>
      <tr>
        <td><a href="<%= @client.identifier %>/urls<%= URI(k).path %>"><%= k %></a></td>
        <td class="text-right"><%= v %></td>
      </tr>
    <% end %>
  </tbody>
</table>
