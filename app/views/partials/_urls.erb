<div class='container'>
  <table class='table table-hover'>
    <thead>
      <tr>
        <th>URLs</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @client.urls.ordered_by_requested.each do |k, v| %>
        <tr>
          <td><%= k %></td>
          <td class="text-right"><%= v %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
