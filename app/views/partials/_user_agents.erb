<div class='container'>
  <table class='table table-hover'>
    <thead>
      <tr>
        <th>Browser Breakdown</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @client.agents.browser_breakdown.each do |k, v| %>
        <tr>
          <td><%= k %></td>
          <td class="text-right"><%= v %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<div class='container'>
  <table class='table table-hover'>
    <thead>
      <tr>
        <th>OS Breakdown</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @client.agents.platform_breakdown.each do |k, v| %>
        <tr>
          <td><%= k %></td>
          <td class="text-right"><%= v %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
